[package]
name = "smt-core"
version = "0.1.0"
edition = "2021"
description = "Pure logic for SMT Task Manager (no tauri dependency, unit-testable)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
